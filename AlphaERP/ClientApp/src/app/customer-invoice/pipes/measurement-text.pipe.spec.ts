import { MeasurementTextPipe } from './measurement-text.pipe';

describe('MeasurementTextPipe', () => {
  it('create an instance', () => {
    const pipe = new MeasurementTextPipe();
    expect(pipe).toBeTruthy();
  });
});
